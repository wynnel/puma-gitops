---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: gateway-test
  namespace: test
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.chart-image: glob:test-*
spec:
  releaseName: gateway-test
  chart:
    git: git@github.com:wynnel/puma-gitops
    path: charts/gateway
    ref: master
  values:
    replicaCount: 1
    image:
      imageName: docker.sandbox2.apimgcp.com/repository/docker-hosted/gateway
      tag: 3
    imageCredentials:
      name: "docker.sandbox2.apimgcp.com"
      registry: "docker.sandbox2.apimgcp.com"
      username: "admin"
      password: "asdf22345asASDFA"
    hazelcast:
      enabled: false
    influxdb:
      host: "apim-service-metrics-runtime-influxdb.default"
    gateway:
      license:
        accept: "true"
      env:
        ENV.PROPERTY.gateway.template: "Testing environment"
        ENV.CONTEXT_PROPERTY.env-configuration.example: "Testing environment"
        ENV.PASSWORD.password: "less secret test password"


