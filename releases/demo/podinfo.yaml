---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: podinfo-demo
  namespace: demo
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.chart-image: glob:dev-*
spec:
  releaseName: podinfo-demo
  chart:
    git: git@github.com:wynnel/gitops-helm
    path: charts/podinfo
    ref: master
  values:
    image: stefanprodan/podinfo:dev-hdtwcel9
    replicaCount: 1
    hpa:
      enabled: false
